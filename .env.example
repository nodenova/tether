# Tether configuration
# Copy to .env and customize

# Required: directories the AI agent is allowed to work in (comma-separated)
TETHER_APPROVED_DIRECTORIES=["/path/to/your/project"]
# Multi-project example:
# TETHER_APPROVED_DIRECTORIES=["/path/to/project1", "/path/to/project2"]

# Max conversation turns per request (default: 25)
# TETHER_MAX_TURNS=25

# Optional system prompt override
# TETHER_SYSTEM_PROMPT="You are a helpful coding assistant."

# Policy files to load (comma-separated paths, default: built-in default.yaml)
# TETHER_POLICY_FILES=policies/default.yaml,policies/custom.yaml

# Dev tools overlay is auto-loaded by default alongside default.yaml.
# To disable it, set TETHER_POLICY_FILES without dev-tools.yaml:
# TETHER_POLICY_FILES=tether/policies/default.yaml

# Approval timeout in seconds (default: 300)
# TETHER_APPROVAL_TIMEOUT_SECONDS=300

# Log level (default: INFO)
# TETHER_LOG_LEVEL=INFO

# Persistent log directory — set to enable file logging (default: disabled)
# Logs are JSON lines written to {dir}/tether.log with rotation.
# TETHER_LOG_DIR=./logs

# Log rotation: max file size in bytes (default: 10MB) and backup count (default: 5)
# TETHER_LOG_MAX_BYTES=10485760
# TETHER_LOG_BACKUP_COUNT=5

# Audit log file path (default: audit.jsonl)
# TETHER_AUDIT_LOG_PATH=audit.jsonl

# Streaming responses — progressively update Telegram messages as agent streams
# TETHER_STREAMING_ENABLED=true
# TETHER_STREAMING_THROTTLE_SECONDS=1.5

# Rate limiting (default: disabled)
# TETHER_RATE_LIMIT_RPM=0
# TETHER_RATE_LIMIT_BURST=5

# Session storage — "memory" (default, lost on restart) or "sqlite" (persistent)
# TETHER_STORAGE_BACKEND=memory
# TETHER_STORAGE_PATH=tether.db

# Tool allow/deny lists (default: all tools allowed)
# TETHER_ALLOWED_TOOLS=
# TETHER_DISALLOWED_TOOLS=

# Telegram connector (optional — enables remote access via Telegram bot)
# Get a token from @BotFather on Telegram
# TETHER_TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# TETHER_ALLOWED_USER_IDS=
